
  # SanaVieta

  This is a code bundle for SanaVieta. The original project is available at https://www.figma.com/design/w6OMCcjTal1gEqJW93khKV/SanaVieta.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  